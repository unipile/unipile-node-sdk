async function main() {
  try {
    // sending message scenario
    // get all chats of a certain provider with a limit
    // send message
  } catch (error) {
    console.error('App error:', error);
  }
}
